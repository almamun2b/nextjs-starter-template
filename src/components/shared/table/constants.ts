/**
 * Shared geometry tokens.
 *
 * The skeleton and the live table must agree on these exactly, otherwise
 * swapping one for the other shifts the layout.
 */
const TABLE_ROW_HEIGHT_CLASS = 'h-16'

const TABLE_HEAD_HEIGHT_CLASS = 'h-11'

const TABLE_MIN_WIDTH_CLASS = 'min-w-5xl'

export {
  TABLE_HEAD_HEIGHT_CLASS,
  TABLE_MIN_WIDTH_CLASS,
  TABLE_ROW_HEIGHT_CLASS,
}
